node_modules/
.env*
*.log
data/

# Supabase local stack — never commit local secrets or generated state
supabase/.branches/
supabase/.temp/

# editor / OS cruft
.idea/
.DS_Store

# generated: knowledge graph and impeccable session scratch (design.json stays tracked)
graphify-out/
.impeccable/live/
.impeccable/critique/

# stray Deno lockfile from running `deno check` at the root; the functions bundle without it
deno.lock
